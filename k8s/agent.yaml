apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kure-monitor-agent
  namespace: kure-system
  labels:
    app: kure-agent
spec:
  selector:
    matchLabels:
      app: kure-agent
  template:
    metadata:
      labels:
        app: kure-agent
    spec:
      serviceAccountName: kure-agent
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      # Ensure the agent runs on all nodes, including control plane nodes if needed
      tolerations:
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
        effect: NoSchedule
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule
      containers:
      - name: agent
        image: AGENT_IMAGE
        imagePullPolicy: IfNotPresent
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: var-tmp
          mountPath: /var/tmp
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
        envFrom:
        - configMapRef:
            name: kure-config
        env:
        - name: BACKEND_URL
          value: "http://kure-monitor-backend:8000"
        - name: PENDING_GRACE_PERIOD
          value: "30"
        - name: FAILURE_LOGS_ENABLED
          value: "true"
        - name: FAILURE_LOGS_MAX_LINES
          value: "1000"
        - name: PYTHONUNBUFFERED
          value: "1"
        # Service-to-service auth: sent as X-Service-Token header on HTTP and
        # ?token=<value> on the WebSocket. Must match backend's SERVICE_TOKEN.
        - name: SERVICE_TOKEN
          valueFrom:
            secretKeyRef:
              name: kure-bootstrap
              key: service-token
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
      volumes:
      - name: tmp
        emptyDir: {}
      - name: var-tmp
        emptyDir: {}
